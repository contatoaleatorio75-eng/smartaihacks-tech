---
interface Props {
    slotId?: string;
    format?: 'auto' | 'rectangle' | 'horizontal';
    className?: string;
}

const { slotId = "0000000000", format = "auto", className = "" } = Astro.props;
---

<div class={`bg-darker border border-dashed border-gray-700 rounded-lg flex items-center justify-center p-4 my-8 min-h-[100px] ${className}`}>
    <div class="text-center">
        <span class="text-xs font-mono text-gray-500 block mb-2">ADVERTISEMENT</span>
        <!-- AdSense Code Placeholder -->
        <!--
        <ins class="adsbygoogle"
             style="display:block"
             data-ad-client="ca-pub-XXXXXXXXXXXXXXXX"
             data-ad-slot={slotId}
             data-ad-format={format}
             data-full-width-responsive="true"></ins>
        <script>
             (adsbygoogle = window.adsbygoogle || []).push({});
        </script>
        -->
        <p class="text-gray-600 text-sm">Ad Space</p>
    </div>
</div>
